# Bank compiler (CONTRACTS.md §9). Copy to .env and fill in. Never commit values.
HF_TOKEN=
ANTHROPIC_API_KEY=
# Optional: write compiled banks straight into ClickHouse after compiling.
CLICKHOUSE_URL=http://localhost:8123
