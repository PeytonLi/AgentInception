# Names only - never commit values (CONTRACTS §9).
HF_TOKEN=
CLICKHOUSE_URL=http://localhost:8123
# Filesystem fallback when ClickHouse is unreachable (banks/manifest.json lives here)
BANKS_DIR=../../banks
MODEL_ID=meta-llama/Llama-3.1-8B-Instruct
INFERENCE_PORT=8000
