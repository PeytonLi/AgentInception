[pytest]
testpaths = tests/integration
asyncio_mode = auto
markers =
    slow: mark test as slow to run (requires --run-slow)
    gpu: mark test as requiring a CUDA GPU (requires --run-gpu)
timeout = 120
