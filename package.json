{
  "name": "ghostbrowser-os",
  "private": true,
  "version": "0.1.0",
  "description": "GhostBrowser OS — web agent with pre-computed latent KV banks (Memory Inception)",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=20"
  }
}
